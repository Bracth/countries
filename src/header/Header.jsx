import StyledHeader from "./StyledHeader";

const Header = () => {
  return (
    <StyledHeader>
      <h1>Where in the world?</h1>
      <span>Dark Mode</span>
    </StyledHeader>
  );
};

export default Header;
